// This is a generated file, modify: generate/templates/templates/struct_content.cc

#include <nan.h>
#include <string.h>
#ifdef WIN32
#include <windows.h>
#else
#include <unistd.h>
#endif // win32

extern "C" {
  #include <git2.h>
 }

#include <iostream>
#include "../include/nodegit.h"
#include "../include/lock_master.h"
#include "../include/functions/copy.h"
#include "../include/worktree_prune_options.h"
#include "nodegit_wrapper.cc"

 
using namespace v8;
using namespace node;
using namespace std;


// generated from struct_content.cc
GitWorktreePruneOptions::GitWorktreePruneOptions() : NodeGitWrapper<GitWorktreePruneOptionsTraits>(NULL, true, v8::Local<v8::Object>())
{
        git_worktree_prune_options wrappedValue = GIT_WORKTREE_PRUNE_OPTIONS_INIT;
      this->raw = (git_worktree_prune_options*) malloc(sizeof(git_worktree_prune_options));
      memcpy(this->raw, &wrappedValue, sizeof(git_worktree_prune_options));
  
  this->ConstructFields();
}

GitWorktreePruneOptions::GitWorktreePruneOptions(git_worktree_prune_options* raw, bool selfFreeing, v8::Local<v8::Object> owner)
 : NodeGitWrapper<GitWorktreePruneOptionsTraits>(raw, selfFreeing, owner)
{
  this->ConstructFields();
}

GitWorktreePruneOptions::~GitWorktreePruneOptions() {
       }

void GitWorktreePruneOptions::ConstructFields() {
       }

void GitWorktreePruneOptions::InitializeComponent(v8::Local<v8::Object> target) {
  Nan::HandleScope scope;

  v8::Local<FunctionTemplate> tpl = Nan::New<FunctionTemplate>(JSNewFunction);

  tpl->InstanceTemplate()->SetInternalFieldCount(1);
  tpl->SetClassName(Nan::New("WorktreePruneOptions").ToLocalChecked());

      Nan::SetAccessor(tpl->InstanceTemplate(), Nan::New("version").ToLocalChecked(), GetVersion, SetVersion);
        Nan::SetAccessor(tpl->InstanceTemplate(), Nan::New("flags").ToLocalChecked(), GetFlags, SetFlags);
   
  InitializeTemplate(tpl);

  v8::Local<Function> _constructor_template = Nan::GetFunction(tpl).ToLocalChecked();
  constructor_template.Reset(_constructor_template);
  Nan::Set(target, Nan::New("WorktreePruneOptions").ToLocalChecked(), _constructor_template);
}

    NAN_GETTER(GitWorktreePruneOptions::GetVersion) {

      GitWorktreePruneOptions *wrapper = Nan::ObjectWrap::Unwrap<GitWorktreePruneOptions>(info.This());

        info.GetReturnValue().Set(Nan::New<Number>(wrapper->GetValue()->version));
     }

    NAN_SETTER(GitWorktreePruneOptions::SetVersion) {
      GitWorktreePruneOptions *wrapper = Nan::ObjectWrap::Unwrap<GitWorktreePruneOptions>(info.This());

         if (value->IsNumber()) {
          wrapper->GetValue()->version = (unsigned int) Nan::To<int32_t>(value).FromJust();
        }
     }

      NAN_GETTER(GitWorktreePruneOptions::GetFlags) {

      GitWorktreePruneOptions *wrapper = Nan::ObjectWrap::Unwrap<GitWorktreePruneOptions>(info.This());

        info.GetReturnValue().Set(Nan::New<Number>(wrapper->GetValue()->flags));
     }

    NAN_SETTER(GitWorktreePruneOptions::SetFlags) {
      GitWorktreePruneOptions *wrapper = Nan::ObjectWrap::Unwrap<GitWorktreePruneOptions>(info.This());

         if (value->IsNumber()) {
          wrapper->GetValue()->flags = (uint32_t) Nan::To<int32_t>(value).FromJust();
        }
     }

   
// force base class template instantiation, to make sure we get all the
// methods, statics, etc.
template class NodeGitWrapper<GitWorktreePruneOptionsTraits>;
