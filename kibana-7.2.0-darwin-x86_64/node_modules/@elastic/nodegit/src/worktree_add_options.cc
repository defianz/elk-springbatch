// This is a generated file, modify: generate/templates/templates/struct_content.cc

#include <nan.h>
#include <string.h>
#ifdef WIN32
#include <windows.h>
#else
#include <unistd.h>
#endif // win32

extern "C" {
  #include <git2.h>
 }

#include <iostream>
#include "../include/nodegit.h"
#include "../include/lock_master.h"
#include "../include/functions/copy.h"
#include "../include/worktree_add_options.h"
#include "nodegit_wrapper.cc"

 
using namespace v8;
using namespace node;
using namespace std;


// generated from struct_content.cc
GitWorktreeAddOptions::GitWorktreeAddOptions() : NodeGitWrapper<GitWorktreeAddOptionsTraits>(NULL, true, v8::Local<v8::Object>())
{
        git_worktree_add_options wrappedValue = GIT_WORKTREE_ADD_OPTIONS_INIT;
      this->raw = (git_worktree_add_options*) malloc(sizeof(git_worktree_add_options));
      memcpy(this->raw, &wrappedValue, sizeof(git_worktree_add_options));
  
  this->ConstructFields();
}

GitWorktreeAddOptions::GitWorktreeAddOptions(git_worktree_add_options* raw, bool selfFreeing, v8::Local<v8::Object> owner)
 : NodeGitWrapper<GitWorktreeAddOptionsTraits>(raw, selfFreeing, owner)
{
  this->ConstructFields();
}

GitWorktreeAddOptions::~GitWorktreeAddOptions() {
       }

void GitWorktreeAddOptions::ConstructFields() {
       }

void GitWorktreeAddOptions::InitializeComponent(v8::Local<v8::Object> target) {
  Nan::HandleScope scope;

  v8::Local<FunctionTemplate> tpl = Nan::New<FunctionTemplate>(JSNewFunction);

  tpl->InstanceTemplate()->SetInternalFieldCount(1);
  tpl->SetClassName(Nan::New("WorktreeAddOptions").ToLocalChecked());

      Nan::SetAccessor(tpl->InstanceTemplate(), Nan::New("version").ToLocalChecked(), GetVersion, SetVersion);
        Nan::SetAccessor(tpl->InstanceTemplate(), Nan::New("lock").ToLocalChecked(), GetLock, SetLock);
   
  InitializeTemplate(tpl);

  v8::Local<Function> _constructor_template = Nan::GetFunction(tpl).ToLocalChecked();
  constructor_template.Reset(_constructor_template);
  Nan::Set(target, Nan::New("WorktreeAddOptions").ToLocalChecked(), _constructor_template);
}

    NAN_GETTER(GitWorktreeAddOptions::GetVersion) {

      GitWorktreeAddOptions *wrapper = Nan::ObjectWrap::Unwrap<GitWorktreeAddOptions>(info.This());

        info.GetReturnValue().Set(Nan::New<Number>(wrapper->GetValue()->version));
     }

    NAN_SETTER(GitWorktreeAddOptions::SetVersion) {
      GitWorktreeAddOptions *wrapper = Nan::ObjectWrap::Unwrap<GitWorktreeAddOptions>(info.This());

         if (value->IsNumber()) {
          wrapper->GetValue()->version = (unsigned int) Nan::To<int32_t>(value).FromJust();
        }
     }

      NAN_GETTER(GitWorktreeAddOptions::GetLock) {

      GitWorktreeAddOptions *wrapper = Nan::ObjectWrap::Unwrap<GitWorktreeAddOptions>(info.This());

        info.GetReturnValue().Set(Nan::New<Number>(wrapper->GetValue()->lock));
     }

    NAN_SETTER(GitWorktreeAddOptions::SetLock) {
      GitWorktreeAddOptions *wrapper = Nan::ObjectWrap::Unwrap<GitWorktreeAddOptions>(info.This());

         if (value->IsNumber()) {
          wrapper->GetValue()->lock = (int) Nan::To<int32_t>(value).FromJust();
        }
     }

   
// force base class template instantiation, to make sure we get all the
// methods, statics, etc.
template class NodeGitWrapper<GitWorktreeAddOptionsTraits>;
